const pontUsuario = 9958;
const nivelUsuario = pontUsuario >= 1000 ? 'Usuário VIP' : 'Usuário Comum';
console.log(nivelUsuario);

/*
if (pontUsuario >= 1000) {
    console.log('Usuário VIP');
} else {
    console.log('Usuário Comum');
} */
